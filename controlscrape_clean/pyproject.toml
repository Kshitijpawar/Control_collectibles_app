[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "controlscrape-clean"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "httpx>=0.28.1",
    "markdownify>=1.2.3",
    "scrapy>=2.17.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

